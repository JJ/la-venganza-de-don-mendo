#!/usr/bin/perl

use strict;
use warnings;

use Don::Mendo;

use POE;

POE::Session->create(
		     inline_states => {
			 _start => \&a_escena,
			 _stop => \&a_actuar,
		     },
		     );


sub a_escena {
    my (
    my $jornadas = Don::Mendo->new()->jornadas();

    
}
